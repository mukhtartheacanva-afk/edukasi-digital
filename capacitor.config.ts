import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  "appId": "com.sidugi.app",
  "appName": "SI-DUGI",
  "webDir": "out",
  "server": {
    "url": "https://edukasi-digital.vercel.app",
    "cleartext": true
  }
}

export default config;
